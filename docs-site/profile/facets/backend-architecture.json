{
  "audience": "backend-architecture",
  "summary": "mountainash-rules provides two complementary rule evaluation architectures under core/ + engines/{filter,accumulator}/ that share a unified dimension model and expression system. Both engines are backend-agnostic through the mountainash.relations and mountainash.expressions layers, enabling operation across Polars, Ibis, and Narwhals-backed DataFrames. A rich ternary logic system powers sentinel-aware matching with full observability, and backend purity is enforced package-wide with exactly three documented exceptions.",
  "concepts": [
    {
      "name": "Shared dimension model",
      "module_ids": [
        "mountainash_rules.core.dimension",
        "mountainash_rules.core.constants"
      ],
      "description": "DimensionsMetadata serves as the single source of truth for both engines, with YAML round-trip and table-level hit-policy fields. It encapsulates dimension definitions, match strategies, and sentinel configuration, providing a consistent contract that decouples rule semantics from evaluation strategy."
    },
    {
      "name": "Filter engine: expression compilation and evaluation",
      "module_ids": [
        "mountainash_rules.core.compiler",
        "mountainash_rules.engines.filter.engine"
      ],
      "description": "DimensionCompiler translates each dimension's MatchStrategy (12 strategies) into a backend-agnostic BaseExpressionAPI expression. ExpressionRulesEngine then evaluates these compiled expressions against a context (evaluate), a batch of contexts (evaluate_batch), or scores every rule unfiltered (explain) via mountainash.relations.Relation, producing ternary match columns, survival flags, and specificity scores in a single vectorized pass."
    },
    {
      "name": "Accumulator engine: prime-encoded combination lattice",
      "module_ids": [
        "mountainash_rules.engines.accumulator.engine",
        "mountainash_rules.engines.accumulator.compiler",
        "mountainash_rules.engines.accumulator.primes",
        "mountainash_rules.engines.accumulator.lattice"
      ],
      "description": "Each rule receives a unique prime number as its identity (up to MAX_RULES_PER_PARTITION=10,000 per partition). Rule combinations are represented as the product of their constituent primes, giving every combination a unique fingerprint, bounded to ~15 primes per combination by int64 (LatticeWidthExceededError beyond that). The build phase iteratively expands valid combinations, and a frontier filter prunes dominated entries via modular arithmetic, producing a compact Lattice of all maximal valid combinations."
    },
    {
      "name": "Ternary partition routing meta-engine",
      "module_ids": [
        "mountainash_rules.engines.accumulator.lattice",
        "mountainash_rules.core.constants"
      ],
      "description": "LatticeIndex embeds a meta-engine over EXACT_KEY dimensions to route CONTEXT_KEY-partitioned contexts to the correct lattice using the same ternary + specificity semantics as ordinary rule evaluation, rather than a separate ad hoc dispatch mechanism -- keeping the routing layer consistent with the rest of the architecture."
    },
    {
      "name": "Backend-agnostic protocol via mountainash.relations",
      "module_ids": [
        "mountainash_rules.engines.filter.engine",
        "mountainash_rules.core.result",
        "mountainash_rules.engines.accumulator.engine"
      ],
      "description": "All DataFrame operations flow through the Relation protocol, which abstracts over concrete backends. relation(df) wraps any supported DataFrame, and collect() materialises results back to the caller's native format. This design allows the same rule definitions and evaluation logic to run on Polars, Ibis, or Narwhals-Pandas backends."
    },
    {
      "name": "Ternary logic and sentinel-aware expressions",
      "module_ids": [
        "mountainash_rules.core.constants",
        "mountainash_rules.core.compiler",
        "mountainash_rules.core.set_wildcard"
      ],
      "description": "The expression system uses a three-valued logic (match=1, unknown=0, non-match=-1) to handle missing or wildcard rule values gracefully across string, numeric, temporal, bool, and set-typed dimensions. Set-dimension wildcards use an in-band '[sentinel]' list (never null) normalized via core/set_wildcard.py, since bool and set types have no reliable null-as-wildcard convention across backends."
    },
    {
      "name": "Backend purity enforcement and documented exceptions",
      "module_ids": [
        "mountainash_rules.core.compiler",
        "mountainash_rules.engines.accumulator.engine",
        "mountainash_rules.engines.accumulator.lattice"
      ],
      "description": "No module under src/mountainash_rules/ may import polars/ibis/narwhals directly. tests/test_backend_purity.py parametrises every non-dunder module with no exemptions. Exactly three '# allow:' escapes are documented: the per-row REGEX fallback (pending upstream regex_contains column-pattern support), the empty-build schema seed in the accumulator engine (pending backend-agnostic empty-frame support), and the snapshot parquet read in Lattice.load (pending backend-agnostic parquet.read_table)."
    },
    {
      "name": "CTX_PREFIX column injection pattern",
      "module_ids": [
        "mountainash_rules.engines.filter.engine",
        "mountainash_rules.core.constants"
      ],
      "description": "Context values are bound into the evaluation frame as literal columns with a __ctx_ prefix. The compiler references these prefixed columns by convention, enabling a clean separation between rule metadata columns and runtime context values within a single DataFrame operation."
    }
  ],
  "featured_modules": [
    "mountainash_rules.engines.filter.engine",
    "mountainash_rules.core.compiler",
    "mountainash_rules.engines.accumulator.engine",
    "mountainash_rules.engines.accumulator.compiler",
    "mountainash_rules.engines.accumulator.primes",
    "mountainash_rules.engines.accumulator.lattice",
    "mountainash_rules.core.constants",
    "mountainash_rules.core.result",
    "mountainash_rules.core.set_wildcard"
  ],
  "hidden_or_internal_modules": [
    "mountainash_rules.core.context",
    "mountainash_rules.__version__"
  ],
  "open_questions": []
}
